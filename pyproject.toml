[project]
name = "news-notifier-crewai"
version = "0.1.0"
dependencies = [
    "crewai",
    "litellm",
    "gspread",
    "requests",
    "beautifulsoup4"
]

[tool.vercel]
entrypoint = "api.cron:handler"